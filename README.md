## Docker system for yade package building
To deploy, simply checkout the repo, copy the conf file in their directories :

- buildall.sh in ~
- dput.cf in ~/.dput.cf
- cron in /etc/cron.d/build-yade-packages

Put the RSA key on the server. Enjoy.
